import DocsHeader from "@/components/blocks/docs-header";
import DocsPreview, { DocsComp } from "@/components/blocks/docs-preview";

<DocsHeader
  title="Chips"
  description="A compact component for displaying information or actions in a small, interactive format."
/>

# Overview

The `Chips` component is a compact UI element designed to display information or actions in a small, interactive format. Chips can represent tags, categories, or actions and are often used for filtering, selection, or displaying attributes in a concise manner.