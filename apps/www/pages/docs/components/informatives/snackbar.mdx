import DocsHeader from "@/components/blocks/docs-header";
import DocsPreview, { DocsComp } from "@/components/blocks/docs-preview";

<DocsHeader
  title="Snackbar"
  description="A brief message that appears temporarily to provide feedback or information to the user."
/>

# Overview

The `Snackbar` component is a transient message that appears at the bottom of the screen to provide feedback or information to the user. It is typically used to inform users about actions they have taken, such as saving data, deleting items, or other notifications that require acknowledgment but do not interrupt the user's workflow.