import DocsHeader from "@/components/blocks/docs-header";
import DocsPreview, { DocsComp } from "@/components/blocks/docs-preview";

<DocsHeader
  title="Tabs"
  description="A user interface element that organizes content into separate views, allowing users to switch between them easily."
/>

# Overview

The `Tabs` component is a user interface element that organizes content into separate views, allowing users to switch between them easily. It is commonly used in applications to categorize information, such as settings, profiles, or any content that can be logically grouped.