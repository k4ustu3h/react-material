import DocsHeader from "@/components/blocks/docs-header";
import DocsPreview, { DocsComp } from "@/components/blocks/docs-preview";

<DocsHeader
  title="Toolbar"
  description="A versatile UI element that provides a horizontal layout for displaying a set of actions or navigation options."
/>

# Overview

The `Toolbar` component is a versatile UI element that provides a horizontal layout for displaying a set of actions or navigation options. It is commonly used in applications to group related actions, such as editing, saving, or navigating through different views.